
import other.*;
public class modifiers {

    public static void main(String[] args) {


    // there are type in the modifiers that says that 
    // dafault , private , protected , public 

    //1 dafault is the modifier that is  placed before the variable or the method that 
    // functions only inside that file 

    // private : if private is placed before the variable only inside that particular class 
    // u can access tha variable 

    // protected : one is that where ths variale with this keyword can be accessed at any file of that package

    // public is the datatype where ever even in different folders u can access the methods

        A obj = new A();
        System.out.println(obj.marks);
int result = obj.show;
    }
}
